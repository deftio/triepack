{
  "name": "triepack-typescript",
  "version": "1.2.0",
  "private": true,
  "description": "In-repo TypeScript wrapper over the JavaScript binding. Not published: the npm package 'triepack' ships the JavaScript implementation with its own type declarations (bindings/javascript/src/index.d.ts).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "@types/jest": "^29.0.0",
    "@types/node": "^20.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  },
  "license": "BSD-2-Clause"
}
